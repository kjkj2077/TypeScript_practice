//tsc -w 터미널에 입력하면 자동으로 Js로 변환됨. 파일사용시는 당연히 js를.
let 이름 = 'kim';
let 이름_어레이 = ['kim', 'park']; //스트링이 들어간 어레이
let 이름_오브젝트 = { name: 'kim' }; //스트링이 들어간 어레이
let 이름숫자 = 'kim'; // 스트링이나 넘버가 들어갈수있다.
let 둘다하기 = 123;
//함수의 경우
function 함수(x) {
    return x * 2;
}
let john = [123, true];
let ko = { name: 'kim' };
